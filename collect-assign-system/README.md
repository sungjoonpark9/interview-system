# collect-assign-system (수집·배정형 데모)

이 폴더는 예약코드 기반의 **개인정보 최소화·분리형 인터뷰 운영 구조**를 검토하기 위한 목업입니다. 확정 요구사항은 `REQUIREMENTS_20260816.md`에서 관리합니다.

배경: 테크팀 답변으로 SharePoint 목록 관리, Power Automate 플로우 구성이 베델 계정 없이는 불가능하다는 점이 확정되어, 전체 구조를 Excel + Microsoft Forms + Teams 파일 탭 중심으로 다시 설계했습니다. 자세한 배경은 `2026.07.26_인터뷰시스템_베델계정불가_재구성안.docx` 문서를 참고하세요.

## 데모 화면 구성

- `index.html` — 허브 페이지
- `screens/00-overview.html` — 5단계 흐름 설명
- `screens/01-applicant-submit.html` — 자원봉사자 가능시간 제출·수정
- `screens/02-interviewer-submit.html` — 인터뷰어 가능시간 제출
- `screens/03-admin-workbook.html` — 관리자 워크북(5개 시트 화면) + "배정 실행" 버튼
- `screens/04-helpdesk.html` — FAQ 우선 문의 접수·답변 확인
- `screens/06-callcenter.html` — 콜센터 자원봉사자 문의 처리

이 화면들은 브라우저 `localStorage`(키: `vm2027_collectassign_code_demo_v3`)에 데이터를 저장해 화면 간 연동을 흉내 냅니다. 이름·회중·연락처 대신 무작위 예약코드와 인터뷰어 코드만 사용합니다.

> 중요: 이 버전은 구조를 설명하는 목업입니다. `localStorage`는 같은 브라우저 안에서만 공유되므로 여러 사람의 예약을 중앙에서 실시간 수집하는 운영 시스템은 아닙니다. 실제 운영 전에는 승인된 내부 저장소·API 또는 다른 수집 절차에 연결해야 합니다. 예약코드는 이름·회중을 암호화한 값이 아니라 개인을 역추정할 수 없는 무작위 코드로 발급해야 합니다.

## 실제 배정 로직 코드

`scripts/assignment-office-script.ts` — 실제 Excel Online에 붙여넣어 바로 실행할 수 있는 Office Script(TypeScript)입니다. `assets/store.js`의 `runAssignment()` 함수는 이 스크립트와 동일한 로직을 자바스크립트로 옮긴 것으로, 데모 화면의 "배정 실행" 버튼이 이것을 호출합니다. 두 코드는 같은 설계 문서(재구성안 2-3절)를 기준으로 나란히 작성되었으므로 로직이 일치합니다.

배정 로직 요약:
1. 인터뷰어가 제출한 가능 날짜 × 가능 시간의 모든 조합을 슬롯마스터의 빈 슬롯에 채웁니다(1일 최대건수 초과 없이).
2. 자원봉사자는 가능시간을 적게 제출한 사람부터 우선 배정합니다.
3. 이미 인터뷰어가 배정되지 않은 슬롯, 정원이 찬 슬롯은 건너뜁니다.
4. 끝까지 배정받지 못한 지원자는 "수동조율필요"로 표시합니다.
5. 재실행해도 이미 배정된 건은 건드리지 않아 여러 번 실행해도 안전합니다.

## 실제로 구축할 때 — 체크리스트

1. **Excel 워크북 생성**: `VM_인터뷰_배정관리.xlsx`를 담당자의 OneDrive 또는 Teams 채널 파일함에 생성하고, 시트 5개(①슬롯마스터, ②인터뷰어가용시간, ③지원자접수, ④배정결과, ⑤헬프데스크)를 문서에 정의된 컬럼 그대로 만듭니다.
2. **Forms 3개 생성**: 지원자 신청용, 인터뷰어 가용시간 등록용, 헬프데스크 문의용. 외부 응답 허용 여부는 테넌트 설정에서 미리 확인합니다(Bethel 계정 불필요, 별도 확인 필요).
3. **Office Script 등록**: 워크북을 Excel Online에서 열고 상단 "자동화" 탭 → "새 스크립트"에 `scripts/assignment-office-script.ts` 내용을 그대로 붙여넣고 저장합니다.
4. **Forms 응답 → 시트 반영**: Forms 응답을 다운로드하거나 복사해 ②·③ 시트에 붙여넣습니다(현재는 수동 — Power Automate 자동 연결 없이도 동작하는 구조입니다).
5. **배정 실행**: "자동화" 탭에서 저장한 스크립트를 클릭해 실행합니다. 약 20행당 30초가 걸리므로, 주 1~2회로 몰아서 실행하기보다 하루 여러 번(소량씩) 자주 실행하는 것을 권장합니다. 실행 중에는 다른 담당자가 워크북 데이터를 동시에 수정·추가하지 않도록 안내합니다.
6. **Teams 통합**: 워크북 파일을 팀 채널의 파일함에 올리고, 채널 상단에 "+" → "Excel"로 탭을 추가해 팀원들이 바로 볼 수 있게 합니다(일반 팀원 권한으로 가능).
7. **결과 검증 및 통지**: ④배정결과 시트를 확인하고 "수동조율필요" 건을 우선 처리한 뒤, 담당자가 승인된 별도 채널 또는 JW Hub를 통해 안내합니다.

## 테크팀 답변 반영 (2026.07.27 확인)

`2026.07.27_수집배정형_구현검수_및_테크팀재질의서.docx`로 재질의한 9개 항목 중 8개에 대한 답변을 받아 아래와 같이 확인했습니다.

- Office Script 실행 라이선스: 자동화 생성·관리 자체는 베델 계정이 필요하지만, 이미 만들어진 스크립트는 Power Automate를 통해 게스트(비베델) 계정도 실행할 수 있습니다. 실행 방식은 계정 유형에 따라 다릅니다 — 베델 계정은 워크북 화면에서 스크립트를 직접 실행하고, 그 외 계정은 목록(List)에 항목을 추가해 실행을 트리거합니다.
- Teams 채널 파일 → Excel 탭 추가: 일반적으로 소유자 권한이 필요하지만, 필요 시 개방 요청이 가능합니다.
- Forms 외부 응답: 로그인이나 jwpub 계정 없이도 이미 테넌트 차원에서 허용되어 있어 별도 설정이 필요 없습니다.
- Forms 다중 선택 응답의 Excel 저장 형식: 세미콜론으로 구분된 하나의 셀 값으로 저장됨을 확인했습니다(`scripts/assignment-office-script.ts`의 파싱 로직과 일치 — 코드 변경 불필요).
- 동시 접근 안전성: 스크립트 실행 중 워크북을 보는 것은 안전하지만, 동시에 데이터를 수정·추가하면 문제가 생길 수 있습니다.
- 처리 용량·시간: 명확한 상한선은 없으나 약 20행에 30초 소요 — 실행을 자주(소량씩) 하는 것을 권장합니다.

이 내용은 `screens/03-admin-workbook.html`의 "실제 실행 시 주의사항" 안내와 이 문서 하단 체크리스트에 반영했습니다.

## 운영 방식 확정 (2026.07.30)

워크북 담당자를 베델 계정 보유자 1명(가능하면 백업 1명 포함 2명)으로 지정하는 것으로 확정했습니다. 이유는 다음과 같습니다.

- 베델 계정이 필요한 두 지점(Office Script 최초 등록, 이후 "자동화" 탭에서 실행)을 담당자 본인이 계속 맡으면, 게스트 계정용 우회 실행 경로(Power Automate + List 트리거)가 필요 없습니다.
- 게스트 계정 우회 경로는 "담당자 외 다른 사람도 실행할 수 있어야 한다"거나 "담당자가 자리를 비운 동안에도 실행되어야 한다" 같은 상황에서만 의미가 있는 선택적 옵션이며, 현재는 도입하지 않습니다.
- 다만 약 20행당 30초라는 처리 속도상 실행을 자주(하루 여러 번) 해야 하므로, 담당자 한 명에게 실행이 몰리면 그 사람이 바쁠 때(예: 리허설 직전) 배정이 밀릴 수 있습니다. 인원 여유가 되면 베델 계정 보유자를 2명 확보해 두는 것을 권장하되, 필수는 아닙니다.

실제 구축 절차(담당자 지정부터 Teams 배포까지)는 `2026.07.30_수집배정형_팀즈실구축가이드.docx`를 참고하세요.
